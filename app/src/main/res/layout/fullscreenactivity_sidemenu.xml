<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/side_bar"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="160dp"
        android:contentDescription="@string/navibar"
        android:scaleType="centerCrop"
        android:src="@drawable/navibar" />

    <ImageView
        android:id="@+id/registered_logo_id"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_gravity="end|top"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="10dp"
        app:srcCompat="@drawable/bitcoin_trans"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/sidebar_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="start|top"
        android:layout_marginStart="20dp"
        android:layout_marginTop="105dp"
        android:text="@string/cryptopanel"
        android:textAppearance="@style/TextAppearance.AppCompat.Large.Inverse"
        android:textColor="#E11D1B1B"
        android:textSize="33sp"
        android:textStyle="bold" />

</FrameLayout>